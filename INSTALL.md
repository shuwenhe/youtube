# Chrome 扩展安装指南

## 快速开始

### 步骤 1：打开 Chrome 扩展管理页面

**方法 A：直接输入 URL**
1. 在 Chrome 地址栏输入：`chrome://extensions/`
2. 按 Enter 键进入

**方法 B：通过菜单**
1. 点击 Chrome 菜单（右上角三个点）
2. 选择 "更多工具" > "扩展程序"

### 步骤 2：启用开发者模式

1. 在扩展程序页面右上角，找到 **"开发者模式"** 开关
2. 点击打开（开关应该变成蓝色）

![启用开发者模式]

### 步骤 3：加载项目文件

1. 点击左上角的 **"加载已解压的扩展程序"** 按钮
2. 在文件浏览器中导航到项目文件夹
   - 路径：`/Users/shuwen/youtube`
3. 选择该文件夹，点击"选择"

### 步骤 4：验证安装

安装完成后，你应该看到：
- ✓ "YouTube 中英文双语字幕" 出现在扩展列表中
- ✓ 工具栏上出现扩展图标（CC 图标）
- ✓ 显示版本号 1.0.0

## 使用说明

### 打开扩展控制面板

1. 在 Chrome 工具栏右上角找到扩展图标
2. 点击扩展图标打开控制面板

### 功能说明

**启用/禁用字幕**
- 切换开关控制字幕显示和隐藏

**选择语言**
- 点击按钮选择显示模式：
  - "双语" - 同时显示中英文
  - "中文" - 仅显示中文字幕
  - "英文" - 仅显示英文字幕

**调整字体大小**
- 拖动滑块调整字幕大小（10px - 28px）

### 在 YouTube 上使用

1. 访问 youtube.com
2. 打开任意有字幕的视频
3. 确保视频启用了字幕（点击视频右下角的"CC"按钮）
4. 扩展将自动显示中英文双语字幕

## 常见问题

### Q: 没有看到字幕怎么办？

**A: 请按以下步骤排查：**

1. **检查视频是否有字幕**
   - 点击视频播放器右下角的"CC"（字幕）按钮
   - 确保至少有一个语言的字幕可用

2. **检查字幕语言**
   - 点击"CC"按钮，选择可用的字幕语言
   - 优先查找中文或英文字幕

3. **检查扩展是否启用**
   - 点击工具栏的扩展图标
   - 确保"启用字幕"开关已打开

4. **刷新页面**
   - 按 F5 或 Cmd+R 刷新页面
   - 重新播放视频

5. **检查扩展权限**
   - 在 `chrome://extensions` 页面找到本扩展
   - 点击"详情"查看权限是否正确

### Q: 字幕显示位置不对

**A:** 字幕固定在视频下方显示。这是设计所考虑到的，以避免遮挡视频内容。如果需要自定义位置，可以在后续版本中添加。

### Q: 某些视频没有中文字幕

**A:** YouTube 的字幕由视频上传者提供。如果视频没有中文字幕，扩展也无法显示。你可以：
- 检查是否有自动生成的中文字幕
- 查看英文字幕（如果有的话）
- 向视频上传者请求添加中文字幕

### Q: 字幕加载很慢

**A:** 这可能是因为：
- YouTube 字幕加载延迟（正常现象）
- 网络连接缓慢
- 视频播放器加载未完成

解决方法：
- 等待视频完全加载后再播放
- 刷新页面重试

### Q: 扩展在某些页面不工作

**A:** 确保：
- 你访问的是 youtube.com 或 youtu.be
- 已在 `chrome://extensions` 启用本扩展
- 没有其他扩展冲突

### Q: 如何卸载扩展？

**A:** 
1. 打开 `chrome://extensions/`
2. 找到 "YouTube 中英文双语字幕" 
3. 点击右下角的"删除"按钮
4. 确认删除

## 开发和调试

### 修改代码后如何重新加载？

1. 修改代码文件
2. 在 `chrome://extensions/` 页面找到本扩展
3. 点击"刷新"按钮（左下方的刷新图标）
4. 刷新 YouTube 页面查看更新

### 查看控制台日志

**Content Script 日志：**
1. 打开 YouTube 页面
2. 按 F12 打开开发者工具
3. 在 Console 标签查看日志

**Background Script 日志：**
1. 在 `chrome://extensions/` 找到本扩展
2. 点击"服务工作者"下方的"检查视图"
3. 在打开的开发者工具中查看日志

## 性能和兼容性

### 浏览器要求

- Chrome 88+ （Manifest V3 支持）
- Edge 88+ （基于 Chromium）
- 其他基于 Chromium 的浏览器

### 已知限制

- 仅支持 YouTube 视频
- 字幕来源于 YouTube 提供的轨道
- 不支持自动翻译（需要视频提供者的字幕）
- 双语显示时依赖视频同时提供中英文轨道

## 隐私和权限

本扩展：
- ✓ 不收集个人数据
- ✓ 不访问 YouTube 账户
- ✓ 仅在 YouTube 页面上运行
- ✓ 所有设置存储在本地浏览器

### 权限说明

| 权限 | 用途 |
|-----|------|
| `activeTab` | 获取当前标签页信息 |
| `scripting` | 在页面中注入脚本 |
| `storage` | 保存用户设置 |
| `*://youtube.com/*` | 访问 YouTube 页面 |

## 故障排除

### 扩展图标显示但功能不工作

**解决方案：**
1. 检查 manifest.json 是否有语法错误
2. 在 `chrome://extensions` 查看是否有错误提示
3. 点击"详情"查看任何权限问题
4. 尝试卸载后重新安装

### 页面加载后字幕不显示

**解决方案：**
1. 等待 2-3 秒让脚本初始化
2. 刷新页面（Cmd+R）
3. 检查浏览器控制台是否有错误
4. 尝试不同的 YouTube 视频

### 中文字幕显示为乱码

**解决方案：**
1. 这通常不会发生，但如果发生：
2. 检查系统字体设置
3. 尝试调整字体大小
4. 重启浏览器

## 获取支持

### 开发者信息

- 项目位置：`/Users/shuwen/youtube`
- 项目文件：
  - `manifest.json` - 扩展配置
  - `content-script.js` - 主要逻辑
  - `popup.html/js` - UI 界面
  - `background.js` - 后台服务

### 提交反馈

如遇问题或有建议，可以：
1. 检查项目的 README.md
2. 查阅本文件
3. 检查代码注释

## 下一步

安装完成后，你就可以开始使用了！

1. 访问 [YouTube](https://www.youtube.com)
2. 打开任意视频
3. 点击工具栏的扩展图标
4. 享受中英文双语字幕体验！

---

**祝你使用愉快！** 🎬🌐
